# == Define: openldap::schema
#
#  Represents a schema file in ldap
#
define openldap::schema (
   $name,
   $data 
) {
  # each($data) |$k, $v| {
  #    file_match { 'test':
  #  
  #    } 
  # } 
}
